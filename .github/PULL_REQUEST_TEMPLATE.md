## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `make tests` passes
- [ ] `make lint` and `make typecheck` pass (or `nox -s tests lint typecheck` for the full matrix)
- [ ] Docs/README updated if behavior or public API changed
- [ ] No unrelated changes bundled in
